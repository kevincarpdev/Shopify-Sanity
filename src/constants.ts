import clsx from 'clsx';

export const DEFAULT_BUTTON_STYLES = clsx([
  'flex h-[2.5rem] duration-200 ease-out items-center justify-center overflow-hidden rounded-lg bg-red py-6 px-10 text-sm font-bold text-white disabled:opacity-20',
  'hover:opacity-80',
]);

export const COLLECTION_PAGE_SIZE = 12;
